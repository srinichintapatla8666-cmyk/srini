# Temporary .env file to test PostgreSQL connection with postgres superuser
# After this works, we'll create the adminuser properly

DJANGO_SECRET_KEY=replace-me
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=YOUR_POSTGRES_PASSWORD_HERE
DB_HOST=127.0.0.1
DB_PORT=5432
